spring:
  datasource:
    username: root
    url: jdbc:mysql://localhost:3306/welcome_app?createDatabaseIfNotExist=true
    password:
  jpa:
    generate-ddl: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: true
    hibernate:
      ddl-auto: update